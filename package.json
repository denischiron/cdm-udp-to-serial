{
  "dependencies": {
    "serialport": "^13.0.0"
  },
  "name": "udp2arduino",
  "version": "1.0.0",
  "description": "Listen UDP Port and send events to Serial port for Arduino",
  "main": "UDP_to_serial.js",
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
